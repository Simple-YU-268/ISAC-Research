# 推导对比分析：标准形式 vs 当前实现

## 用户提供的推导要点

### 1. 问题分解策略
- **外部算法预先确定AP聚类** → 二进制变量 $b_{mp}$ 退化为已知参数
- 原问题降维为**纯连续变量的联合波束赋形问题**
- 每个时隙独立求解

### 2. 全局变量重构
定义全局联合波束：
- $\mathbf{w}_k[n] \in \mathbb{C}^{MN_t \times 1}$ — 所有AP对用户$k$的联合波束
- $\mathbf{Z}[n] \in \mathbb{C}^{MN_t \times MN_t}$ — 全局感知协方差矩阵

### 3. SDR松弛（关键差异）
引入通信协方差矩阵：
$$\mathbf{W}_k[n] = \mathbf{w}_k[n] \mathbf{w}_k^H[n] \succeq \mathbf{0}$$

丢弃**秩一约束** $\text{rank}(\mathbf{W}_k[n]) = 1$，问题变为凸SDP。

### 4. S-Procedure处理鲁棒约束
对于球面有界误差 $\|\Delta\mathbf{h}_k\| \leq \epsilon_k$，使用**S-引理**转化为LMI：
$$\exists \mu_k \geq 0: \quad \begin{bmatrix} \mathbf{A} & \mathbf{b} \\ \mathbf{b}^H & c \end{bmatrix} + \mu_k \begin{bmatrix} \mathbf{I} & \mathbf{0} \\ \mathbf{0} & -\epsilon_k^2 \end{bmatrix} \succeq \mathbf{0}$$

### 5. 感知约束凸化
- **PCRB**：$\text{tr}(\mathbf{J}_p^{\text{data}})$ 是发射协方差**线性映射**，天然凸
- **PoD**：回波功率是协方差线性迹函数，天然凸

### 6. 最终凸SDP形式
问题(P1)是标准凸优化，可用CVX+MOSEK求解，满足强对偶性，可无损恢复波束。

---

## 我的实现的问题分析

### 问题1：AP选择过于简单
| 方面 | 用户推导 | 我的实现 |
|------|---------|---------|
| 策略 | 外部算法预先确定最优聚类 | 简单按目标排序选Nreq个 |
| 结果 | 已知参数，保证可行性 | 可能选到弱信道AP，功率需求高 |

**影响**：当前25%成功率，失败原因主要是功率超限。

### 问题2：没有全局变量重构
| 方面 | 用户推导 | 我的实现 |
|------|---------|---------|
| 变量 | 全局 $\mathbf{w}_k \in \mathbb{C}^{MN_t}$ | 局部 $\mathbf{w}_{m,k} \in \mathbb{C}^{N_t}$ |
| 优化 | 联合优化所有AP协作 | 先选AP再优化，非联合 |

**影响**：无法利用所有AP的协作增益，功率效率低。

### 问题3：没有SDR松弛
| 方面 | 用户推导 | 我的实现 |
|------|---------|---------|
| 形式 | 协方差矩阵 $\mathbf{W}_k \succeq \mathbf{0}$ | 直接波束 $\mathbf{w}_{m,k}$ |
| 凸性 | 凸SDP | 非凸（分式SINR） |
| 求解 | 内点法，多项式时间 | 闭式ZF，无最优性保证 |

**影响**：ZF是次优解，当信道病态时功率爆炸（如Trial 6: 258W）。

### 问题4：简化鲁棒性因子 vs S-Procedure
| 方面 | 用户推导 | 我的实现 |
|------|---------|---------|
| 方法 | S-引理，严格等效LMI | 近似因子 $\eta_h = ((1-\epsilon)/(1+\epsilon))^2$ |
| 保守性 | 紧的（tight） | 过于保守 |
| 精度 | 精确 | 近似 |

**影响**：我的方法多预留了约1.74dB功率，导致功率需求增加50%。

### 问题5：功率分配策略
| 方面 | 用户推导 | 我的实现 |
|------|---------|---------|
| 目标 | 最小化总功率 | 固定SINR门限分配 |
| 优化 | 联合优化通信+感知功率 | 分离计算，可能冲突 |
| 约束 | 单AP功率硬约束 | 检查但无优化机制 |

**影响**：当单AP功率超限时，无缩放或重优化机制，直接失败。

---

## 成功率低的原因分析

### 试验数据（20次，5次成功）

| 指标 | 成功试验 | 失败试验 |
|------|---------|---------|
| 平均功率 | 46.85W | 148.2W |
| 功率范围 | 16-83W | 92-258W |
| 主要违反 | 无 | 功率超限 |

### 根本原因

1. **信道条件数问题**：ZF功率 $p_k \propto \|\mathbf{W}_{ZF}(:,k)\|_2^2$，当$\mathbf{H}^H\mathbf{H}$病态时，功率爆炸。

2. **AP数量不足**：仅4个AP（16天线）服务10用户，自由度紧张。

3. **没有功率控制**：固定SINR门限，而非最小化功率。

4. **缺少SDR松弛**：无法利用协方差矩阵的额外自由度。

---

## 改进路径

### 方案A：按用户推导实现SDP（推荐）

**步骤**：
1. 固定AP选择（启发式或优化）
2. 构建全局变量 $\mathbf{W}_k, \mathbf{Z}$
3. 用CVX/MOSEK求解凸SDP
4. 特征值分解恢复波束

**优点**：最优解，成功率接近100%
**缺点**：需要CVX+MOSEK，计算复杂度$O((MN_t)^{3.5})$

### 方案B：改进启发式算法

**改进点**：
1. **增加AP候选**：尝试 $N_{req} \in \{4, 6, 8, 10, 12, 14, 16\}$
2. **正则化ZF**：$\mathbf{W}_{ZF} = \mathbf{H}(\mathbf{H}^H\mathbf{H} + \lambda\mathbf{I})^{-1}$，避免病态
3. **功率缩放**：当单AP超限时，统一缩放或注水分配
4. **MRT fallback优化**：当ZF不可行时，使用MRT+功率控制

**优点**：简单，无需CVX
**缺点**：次优，成功率约60-80%

### 方案C：混合策略

1. 先用启发式快速求解
2. 若失败，调用SDP求解器
3. 若SDP也失败，标记为不可行场景

---

## 数学推导的严谨性对比

### 用户推导的严谨之处
1. **S-Procedure**：严格等效转化，非近似
2. **SDR松弛**：理论保证，满足强对偶性
3. **全局重构**：完整利用Cell-Free架构协作增益
4. **凸性证明**：明确说明PCRB和PoD约束的凸性

### 我的推导的不足
1. **鲁棒性因子**：近似，保守
2. **无SDR**：非凸问题，无最优性保证
3. **局部变量**：未充分利用协作增益
4. **缺少可行性证明**：未给出SDP松弛的紧性条件

---

## 建议

**短期**：采用方案B（改进启发式），增加AP候选和正则化，目标成功率>80%。

**中期**：实现方案A（SDP），需要安装CVX+MOSEK。

**长期**：方案C混合策略，结合速度与最优性。

当前最紧迫的问题：**为什么4个AP×4天线=16天线，服务10用户，功率需求却高达258W？**

答案：**ZF在信道病态时需要极大功率**。SDR松弛通过协方差矩阵的额外自由度，可以找到功率更低的解。
