# Cell-Free ISAC 实验设计方案

## 1. 实验目标

验证优化后的求解器性能，系统评估关键参数对成功率的影响，为实际部署提供参数选择依据。

## 2. 基准配置

| 参数 | 值 | 说明 |
|------|-----|------|
| M | 16 | AP数量 |
| Nt | 4 | 每AP天线数 |
| K | 10 | 用户数 |
| Pmax | 30W | 总功率预算 |
| SNR门限 | 0dB | 感知SNR要求 |
| SINR门限 | 0dB | 通信SINR要求 |
| epsilonH | 0.10 | 通信信道CSI误差 |
| epsilonG | 0.15 | 感知信道CSI误差 |
| 目标分布 | ±50m | 目标位置随机范围 |

## 3. 实验内容

### 实验1: 成功率 vs 感知SNR门限
- 变量: snrReqDb ∈ {-5, -3, 0, 3, 5, 10}
- 固定: 其他所有参数
- 指标: 成功率、平均功率、平均激活AP数
- 目的: 找到SNR门限的"甜蜜点"

### 实验2: 成功率 vs 总功率预算
- 变量: Pmax ∈ {10, 20, 30, 40, 50, 100}
- 固定: 其他所有参数
- 指标: 成功率、功率利用率
- 目的: 评估功率预算的影响

### 实验3: 成功率 vs AP数量
- 变量: M ∈ {4, 8, 12, 16, 20, 25}
- 固定: 其他所有参数
- 指标: 成功率、平均激活AP数
- 目的: 评估AP密度的影响

### 实验4: 成功率 vs 用户数
- 变量: K ∈ {2, 5, 10, 15, 20}
- 固定: 其他所有参数
- 指标: 成功率、SINR分布
- 目的: 评估系统容量

### 实验5: 成功率 vs 目标分布范围
- 变量: targetRange ∈ {20, 30, 40, 50, 60, 80}
- 固定: 其他所有参数
- 指标: 成功率、SNR分布
- 目的: 评估覆盖范围

### 实验6: 成功率 vs CSI误差
- 变量: epsilon ∈ {0, 0.05, 0.10, 0.15, 0.20, 0.30}
- 固定: epsilonH = epsilonG = epsilon
- 指标: 成功率、功率开销
- 目的: 评估鲁棒性代价

### 实验7: 波束成形策略对比
- 策略: ZF vs MRT vs MMSE
- 固定: 其他所有参数
- 指标: 成功率、计算时间、功率效率
- 目的: 验证ZF的优越性

### 实验8: 多种子统计验证
- 变量: seed ∈ {1:10}
- 每个种子运行100次trial
- 指标: 成功率均值、方差、置信区间
- 目的: 统计显著性验证

## 4. 输出格式

每个实验输出:
- 表格: 参数值 vs 成功率 vs 平均功率 vs 平均SNR vs 平均SINR
- 可选: 保存到 results/experiment_*.mat

## 5. 执行计划

1. 编写通用实验框架 `run_experiments.m`
2. 逐个执行实验1-8
3. 汇总结果到 `results/summary.md`
